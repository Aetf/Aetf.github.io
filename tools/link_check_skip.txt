# Some random links in the complex-md-test
http://docutils.sourceforge.net/mirror/setext.html
http://www.aaronsw.com/2002/atx/
http://textism.com/tools/textile/
http://docutils.sourceforge.net/rst.html
http://www.triptico.com/software/grutatxt.html
http://ettext.taint.org/doc/

# CSDN connection is always bad, we know the link is there, so don't bother checking it
csdn\.net

# github.com too many request
https://github.com/Aetf

# Umich links not working but is correct
https://www.eecs.umich.edu/cse/events/2018-ai-symposium/

# The dissertation handle on /research resolves fine in a browser, but Deep Blue sits
# behind Cloudflare bot management: the first hit from a fresh IP gets a 200 and every
# subsequent one gets a 403, so a full crawl (which hits it twice, times 5 retries)
# always fails, and from GitHub Actions IP ranges it is 403 from the start.
hdl\.handle\.net
deepblue\.lib\.umich\.edu

# The two talk recordings linked from /research (Fluid, MLSys'21). YouTube rate-limits
# datacenter IPs, so from GitHub Actions these intermittently come back 302 => HTTP 429.
# Both return 200 from a normal connection. 429 is an HTTP status rather than a connection
# failure, so `--connection-failures-as-warnings` in link_check.sh does not cover it.
https://www\.youtube\.com/watch

# Remove after next-theme/hexo-theme-next@0708dc7e18cc06a80204c0d173799dabcc45a084 is included in a release
# Temp rule that skips bad muse.js link
muse.js
